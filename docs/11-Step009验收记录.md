# Step 009 验收记录

> 执行日期：2026-09-07
>
> 记录状态：`DEPENDENCY_REVIEW_REQUIRED`

## 版本与解析结果

| 项目 | 实际结果 |
|---|---|
| Python | 3.12.10 |
| uv | 0.11.7 |
| Node.js | 20.20.2（仓库 `.runtime/tools/node20`，系统 Node.js 24.15.0 保留） |
| LangGraph | 锁定在 1.x 约束内 |
| 依赖解析 | 131 packages resolved |
| SBOM | CycloneDX JSON，129 components |
| SBOM SHA-256 | `2CC4C981186FE43692EABEEF147CA9A5D081D1E068468116DC5FB660713B6D4D` |
| Compose 镜像 | PostgreSQL、Redis、MinIO 已升级为 OCI digest |

## 已执行检查

```text
uv lock --check                         PASS
uv sync --dev                           PASS
runtime imports                         PASS
uv run ruff check src tests scripts     PASS
uv run mypy src                         PASS
uv run pytest -q                        1 passed
docker compose ... config               PASS
SBOM JSON validation                    PASS
uv run pip-audit                         BLOCKED_NETWORK：PyPI 读取超时
```

SBOM 由 `scripts/generate_sbom.ps1` 基于 `.venv` 和 `pyproject.toml` 生成，项目组件为 `ai-operations-assistant 0.1.0`。SBOM 的 129 个组件对应当前 Windows/Python 3.12 环境；`uv.lock` 中另有 11 个跨平台、构建或规范化名称条目，不属于当前已安装环境，不能简单按原始名称逐字比较。当前本机 Node.js 版本不是目标 20 LTS；该偏差已明确保留，不影响 Python/uv/Compose 依赖锁定结论。

## 遗留项

`uv run --locked pip-audit` 已尝试 PyPI/OSV 服务；本次使用项目内缓存和 10 秒请求超时，最终收到 `HTTPSConnectionPool(host='pypi.org', port=443): Read timed out`，因此本记录不声明“无已知漏洞”，安全依赖审查需在可访问漏洞源的 CI 中补跑。Compose 的 PostgreSQL、Redis、MinIO 已通过 Docker Registry HTTP API 获取并固定 OCI digest，但本机 Docker daemon 未运行，尚未完成本机拉取/健康检查。Node.js 20.20.2 已从固定官方发行 URL 下载、校验 SHA-256 并放置在仓库 `.runtime/tools/node20`，健康检查已通过；该目录被 `.gitignore` 忽略，不进入 Git，其他开发机可使用 `scripts/install_node20.ps1` 重现。状态保持 `DEPENDENCY_REVIEW_REQUIRED`，不代表生产就绪。
