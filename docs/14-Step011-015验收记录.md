# Step 011–015 准备阶段收尾验收记录

> 执行日期：2026-09-07
>
> 记录状态：`FOUNDATION_CLOSEOUT_REVIEW_REQUIRED`

## Step 011：质量和 CI 门禁

已建立 `.github/workflows/ci.yml`，覆盖锁定同步、Ruff、mypy、pytest、pip-audit、secret scan、迁移检查、Alembic upgrade/downgrade、JSON Schema 导出和 Demo runner。本地等价检查全部通过；CI 尚需在实际 GitHub Runner 上运行一次。

## Step 012：本地基础设施

Compose 已固定 PostgreSQL/Redis/MinIO digest、命名卷、backend 网络和容器健康检查。`docker compose ... config` 与静态 `infra_check.py` 通过。当前 Docker daemon 是否可启动需由本机执行 `docker compose up -d` 后补充健康检查和重启持久化证据；SQLite + Mock 离线模式可用。

## Step 013：领域 Schema

已定义 Tenant、Mailbox、Thread、Message、Draft、Approval、FollowUp、ProfileFact、KnowledgeDocument、AgentRun 和 EventEnvelope。Schema 默认拒绝未知字段，事件包含 provider/internal/thread/event ID 和 schema_version；缺字段、未知字段、非法版本、旧版本标识和不可映射事件测试已覆盖，并可导出 11 份版本化 JSON Schema。

## Step 014：数据库迁移

已建立 Alembic 初始迁移和 Tenant、Mailbox、Inbox、Thread、Message、Draft、Approval、FollowUp、ProfileFact、KnowledgeDocument、AgentRun、SendAttempt、AuditEvent 表及唯一约束/索引。SQLite `upgrade head`、`current`、重复 upgrade 和 `downgrade base` 均通过；生产 PostgreSQL 迁移仍需在容器环境复核。

## Step 015：Demo Fixture 和运行手册

已建立 `fixtures/emails`、`fixtures/knowledge`、`fixtures/expected`、`scripts/run_demo.py` 和 `docs/demo-runbook.md`。Demo runner 会逐项校验全部 11 个邮件 fixture 的预期终态，覆盖普通 FAQ、地址查询、高风险、多意图、空正文、HTML、附件元数据、Prompt Injection、超时、重复事件和跟进取消；不依赖残留数据库，不产生外部副作用。

## 当前结论

本地代码、Schema、迁移、Demo 和静态质量检查已完成（Ruff、mypy、pytest 19 passed、secret scan、migration check、infra check、JSON Schema export 11/11、Demo runner 11/11 均通过）；Git 已建立 `codex/foundation-closeout` 分支并创建 `v0.1.0-level0` 标签。真实 CI Runner、Docker daemon 的容器启动/健康/重启持久化测试和生产 PostgreSQL 复核属于待环境执行项。状态保持 `FOUNDATION_CLOSEOUT_REVIEW_REQUIRED`，不宣称 Level 1 Demo 或生产就绪。
