# Step 007 验收记录

> 执行日期：2026-09-07
>
> 记录状态：`ENVIRONMENT_BASELINE_REVIEW_REQUIRED`

## 工具版本

| 工具 | 实际结果 | 基线判断 |
|---|---|---|
| Python | 3.12.10（通过 `py -3.12` 调用） | PASS |
| uv | 0.11.7（用户级路径） | PASS |
| Node.js | 20.20.2（仓库 `.runtime/tools/node20`） | PASS；官方压缩包 SHA-256 已校验，系统 Node.js 24.15.0 未覆盖，健康检查优先使用仓库工具 |
| Docker Engine | 29.4.2 | PASS，满足 24+ |
| Docker Compose | v5.1.3 | PASS，满足 v2 |

## 已执行检查

```text
uv lock --offline                         PASS
pwsh -NoProfile -File scripts/healthcheck.ps1          PASS
pwsh -NoProfile -File scripts/healthcheck.ps1 -RequireDocker PASS
docker compose -f compose.yaml -f compose.dev.yaml config PASS
```

健康检查输出：

```text
HEALTHCHECK_PASS mode=offline database=sqlite adapter=mock
```

## 结论与遗留项

SQLite + Mock 离线模式和严格 Docker/Compose 配置检查均已在本机通过。Node.js 20.20.2 已通过仓库内便携工具和 SHA-256 校验；系统 Node.js 24.15.0 未覆盖。上述结果不等同于 Gmail、飞书、LLM 或生产运行时 Gate 已通过。
